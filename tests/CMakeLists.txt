set(UCP_TSAN_TEST_LAUNCHER)
if(UCP_ENABLE_TSAN AND CMAKE_SYSTEM_NAME STREQUAL "Linux" AND
   CMAKE_SYSTEM_PROCESSOR MATCHES "^(x86_64|amd64|AMD64)$")
    find_program(UCP_SETARCH_EXECUTABLE NAMES setarch)
    if(NOT UCP_SETARCH_EXECUTABLE)
        message(FATAL_ERROR "TSan tests on Linux x86_64 require the setarch executable")
    endif()
    set(UCP_TSAN_TEST_LAUNCHER ${UCP_SETARCH_EXECUTABLE} x86_64 -R)
endif()

function(add_ucp_test target source)
    add_executable(${target} ${source})
    target_link_libraries(${target} PRIVATE proactor_static pthread)
    target_include_directories(${target} PRIVATE ${PROJECT_SOURCE_DIR}/tests)
    add_test(NAME ${target} COMMAND ${UCP_TSAN_TEST_LAUNCHER} $<TARGET_FILE:${target}>)
endfunction()

add_ucp_test(ucp_result_test result_test.cpp)
add_ucp_test(ucp_task_test task_test.cpp)
add_ucp_test(ucp_io_operation_test io_operation_test.cpp)
add_ucp_test(ucp_event_loop_operation_test event_loop_operation_test.cpp)
add_ucp_test(ucp_async_socket_test async_socket_test.cpp)
add_ucp_test(ucp_async_connect_test async_connect_test.cpp)
add_ucp_test(ucp_http_framer_test http_framer_test.cpp)
add_ucp_test(ucp_route_table_test route_table_test.cpp)
add_ucp_test(ucp_upstream_pool_test upstream_pool_test.cpp)
add_ucp_test(ucp_proxy_session_test proxy_session_test.cpp)
add_ucp_test(ucp_gateway_shutdown_test gateway_shutdown_test.cpp)
add_dependencies(ucp_gateway_shutdown_test ucp_gateway)
add_ucp_test(ucp_gateway_failure_test gateway_failure_test.cpp)
